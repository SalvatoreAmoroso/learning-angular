import { PanelHeaderDirective } from './panel-header.directive';

describe('PanelHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
