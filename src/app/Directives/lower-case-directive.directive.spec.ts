import { LowerCaseDirective } from './lower-case-directive.directive';

describe('LowerCaseDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LowerCaseDirective();
    expect(directive).toBeTruthy();
  });
});
