import { LowerCaseDirectiveDirective } from './lower-case-directive.directive';

describe('LowerCaseDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LowerCaseDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
