import { BorderDirective } from './border.directive';

describe('ChBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderDirective();
    expect(directive).toBeTruthy();
  });
});
