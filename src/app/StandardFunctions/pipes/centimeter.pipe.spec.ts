import { CentimeterPipe } from './centimeter.pipe';

describe('CentimeterPipe', () => {
  it('create an instance', () => {
    const pipe = new CentimeterPipe();
    expect(pipe).toBeTruthy();
  });
});
